# secrets/connect/pisey_db.properties
#
# Resolved by Kafka Connect's built-in FileConfigProvider at connector
# registration time. Referenced from connector JSON as:
#   ${file:/opt/kafka/secrets/pisey_db.properties:user}
#   ${file:/opt/kafka/secrets/pisey_db.properties:password}
#
# This file holds real credentials, avoid exposing it.

user=
password=